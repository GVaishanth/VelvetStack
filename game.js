/* Velvet Stack — non-poker game engines. Poker remains in poker.html/app.js. */
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)], params=new URLSearchParams(location.search), kind=params.get('game')||'rummy';
const CONFIG={rummy:{name:'Meld House',kicker:'SEQUENCES & SETS',desc:'Draw, meld, discard. Empty your hand with valid runs and sets.'},ace:{name:'Ace High',kicker:'HIGH CARD HEAT',desc:'Five rounds. Reveal one card each and let the highest card take the point.'},uno:{name:'Color Clash',kicker:'THE COLOR RACE',desc:'Match color or number, use action cards, and call your final card.'},'uno-flip':{name:'UNO Flip!',kicker:'TWO-SIDED CHAOS',desc:'Play through a light side and flip into the darker, higher-penalty side.'}};
if(!['rummy','uno'].includes(kind)) location.href='index.html';
document.body.classList.add(kind);
let mode='solo',game=null,myId=0,peer=null,conn=null,isHost=false,selected=null,pendingWild=null,botTimer=null;
const suits=['♠','♥','♦','♣'], ranks=['2','3','4','5','6','7','8','9','T','J','Q','K','A'];
const $toast=t=>{const e=$('#toast');e.textContent=t;e.classList.add('show');clearTimeout(window.toastTimer);window.toastTimer=setTimeout(()=>e.classList.remove('show'),2300)};
function shuffle(a){for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]]}return a}
function standardDeck(){return shuffle(suits.flatMap(s=>ranks.map((r,i)=>({suit:s,rank:r,value:i+2}))));}
function cardCode(c){const rank=c.rank==='10'?'0':c.rank,suit={'♠':'S','♥':'H','♦':'D','♣':'C'}[c.suit];return `${rank}${suit}`}function cardEl(c,back=false){const e=document.createElement('button');e.className='playing-card '+(back?'card-back':(c.suit==='♥'||c.suit==='♦'?'red':''));if(!back&&c.suit&&c.rank){const img=document.createElement('img');img.src=`cards/playing/${cardCode(c)}.svg`;img.alt=`${c.rank} of ${c.suit}`;img.loading='lazy';img.onerror=()=>{img.remove();e.insertAdjacentHTML('beforeend',`<b>${c.rank}</b><small>${c.suit}</small>`)};e.append(img)}else if(!back)e.innerHTML=`<b>${c.rank}</b><small>${c.suit||''}</small>`;return e}
function showGame(){ $('#lobby').classList.add('hidden');$('#game').classList.remove('hidden') }
function drawCard(){if(!game.deck.length){if(game.discard.length>1){const top=game.discard.pop();game.deck=shuffle(game.discard);game.discard=[top]}else game.deck=kind==='ace'?standardDeck():unoDeck()}return game.deck.pop()}
function setConfig(){const c=CONFIG[kind];$('#gameKicker').textContent=c.kicker;$('#gameTitle').textContent=c.name;$('#gameDescription').textContent=c.desc;$('#gameBarKicker').textContent=c.kicker;$('#gameBarTitle').textContent=c.name;document.title=`${c.name} — Velvet Stack`}
function newGame(networkMode='solo'){mode=networkMode;selected=null;const botCount=Math.max(2,Math.min(8,Number($('#botCount')?.value)||3)),count=networkMode==='solo'?botCount+1:4,names=[$('#playerName').value.trim()||'You','Maya','Theo','Ari','Sam','Lina','Ravi','Nora','Quinn'];game={type:kind,players:Array.from({length:count},(_,i)=>({name:names[i]||`Bot ${i}`,hand:[],score:0,money:1000,finished:false,bot:i>0}),),deck:[],discard:[],turn:0,round:1,maxRounds:5,winner:null,phase:'play',color:null,dark:false,drawStack:0,botLevel:$('#botLevel')?.value||'sharp',botSpeed:$('#botSpeed')?.value||'turbo'};if(mode==='online'&&game.players[1])game.players[1].bot=false;if(kind==='rummy')setupRummy();else if(kind==='ace')setupAce();else setupUno();showGame();render();if(mode==='solo'&&game.turn===1)queueBot()}
function rummyDeck(){let copies=game.players.length>=5?2:1,all=[];for(let i=0;i<copies;i++)all.push(...standardDeck());return shuffle(all)}function setupRummy(){game.handSize=game.players.length===2?13:game.players.length<=4?10:game.players.length<=6?7:4;game.entryFee=100;game.pot=game.players.length*game.entryFee;game.finishOrder=[];game.rummyOver=false;game.lastAction=`All players ante $${game.entryFee}. Pot is $${game.pot}.`;game.deck=rummyDeck();game.players.forEach(p=>{p.hand=game.deck.splice(0,game.handSize);p.money-=game.entryFee;p.finished=false});game.discard=[game.deck.pop()];game.phase='draw'}
function setupAce(){game.deck=standardDeck();game.round=1;game.maxRounds=5;game.phase='reveal';game.players.forEach(p=>p.hand=[drawCard()])}
function unoDeck(){const colors=game?.dark?['pink','teal','orange','purple']:['red','yellow','green','blue'];let d=[];colors.forEach(color=>{d.push({color,value:0,type:'number'});for(let n=1;n<=9;n++)d.push({color,value:n,type:'number'},{color,value:n,type:'number'});['skip','reverse','draw2'].forEach(type=>{d.push({color,value:type,type},{color,value:type,type})})});const wildCount=game?.dark?4:4;for(let i=0;i<wildCount;i++)d.push({color:'wild',value:'wild',type:'wild'},{color:'wild',value:'wild4',type:'wild4'});return shuffle(d)}
function setupUno(){game.dark=kind==='uno-flip'?false:false;game.deck=unoDeck();game.players.forEach(p=>p.hand=game.deck.splice(0,7));let first=drawCard();while(first.type==='wild4')game.deck.unshift(first),first=drawCard();game.discard=[first];game.color=first.color;game.turn=0;game.phase='play'}
function current(){return game.players[game.turn]}function wireMovable(hand){const box=$('#hand');[...box.children].forEach((el,i)=>{el.draggable=true;el.dataset.index=i;el.addEventListener('dragstart',e=>{e.dataTransfer.setData('text/plain',String(i));el.classList.add('dragging')});el.addEventListener('dragend',()=>el.classList.remove('dragging'));el.addEventListener('dragover',e=>e.preventDefault());el.addEventListener('drop',e=>{e.preventDefault();const from=Number(e.dataTransfer.getData('text/plain')),to=Number(el.dataset.index);if(Number.isInteger(from)&&Number.isInteger(to)&&from!==to){const [card]=hand.splice(from,1);hand.splice(to,0,card);selected=null;render()}})})}
function canAct(){return game&&game.winner===null&&game.turn===myId}
function sendAction(action,data={}){if(mode==='online'&&!isHost){conn?.send({type:'action',action,data});return false}return true}
function sync(){if(!isHost||!conn?.open)return;const view=JSON.parse(JSON.stringify(game));if(view.winner===null){view.players.forEach((p,i)=>{if(i!==1)p.hand=[]})}conn.send({type:'state',game:view})}
function render(){if(!game)return;$('#roomLabel').textContent=mode==='online'?(isHost?'HOST ROOM':'ONLINE PLAYER'):'';if(kind==='rummy')renderRummy();else if(kind==='ace')renderAce();else renderUno()}
function renderRummy(){const me=game.players[myId];$('#status').textContent=game.rummyOver?`Hand settled — ${game.finishOrder.map((x,i)=>`${i+1}. ${x.name}`).join(' · ')}`:game.turn!==myId?`${current().name} is thinking…`:game.phase==='draw'?'Draw from the deck or take the top discard.':'Select one card to discard.';$('#board').innerHTML=game.players.map((p,i)=>`<div class="board-stat seat-stat ${i===myId?'you':''} ${p.finished?'finished-seat':''}"><b>${p.hand.length}</b><span>${p.name}<br>$${p.money}</span></div>`).join('')+`<div class="board-stat deck-stat"><b>${game.handSize}</b><span>${game.handSize} cards · ante $${game.entryFee} · pot $${game.pot}</span></div><div id="rummyDiscard"></div><div class="board-stat"><b>${canWinRummy(me.hand)?'WINNING HAND':canMeldAll(me.hand)?'MELDS READY':'BUILD MELDS'}</b><span>hand check</span></div>`;$('#rummyDiscard').replaceChildren(cardEl(game.discard.at(-1)));const activity=document.createElement('div');activity.className='rummy-activity';activity.innerHTML=`<small>TABLE ACTION</small><strong>${game.lastAction||'The table is ready.'}</strong>`;const opponentHands=document.createElement('div');opponentHands.className='opponent-hands';game.players.filter((p,i)=>i!==myId).forEach(p=>{const seat=document.createElement('div');seat.className='opponent-hand';seat.innerHTML=`<span>${p.name} · ${p.hand.length} cards</span>`;const backs=document.createElement('div');backs.className='back-row';for(let i=0;i<Math.min(p.hand.length,6);i++)backs.append(cardEl(null,true));seat.append(backs);opponentHands.append(seat)});$('#board').append(activity,opponentHands);if(game.rummyOver){const reveal=document.createElement('div');reveal.className='rummy-showdown';reveal.innerHTML='<div class="showdown-title">SHOWDOWN · ALL HANDS REVEALED</div>';game.players.forEach(p=>{const row=document.createElement('div');row.className='reveal-row '+(p.name===game.winner?'winning-row':'');const label=document.createElement('div');label.className='reveal-name';label.innerHTML=`<strong>${p.name}</strong><small>${game.finishOrder?.findIndex(x=>x.name===p.name)===0?'WINNER':game.finishOrder?.findIndex(x=>x.name===p.name)>=0?'RUNNER UP':'LOSER'}</small>`;const cards=document.createElement('div');cards.className='reveal-cards';const meldCards=(findMeldPlan(p.hand)||[]).flat();p.hand.forEach((c,i)=>{const e=cardEl(c);e.classList.toggle('meld-card',meldCards.includes(c));if(p.name===game.winner){e.style.animationDelay=`${i*90}ms`;e.classList.add('winner-card')}cards.append(e)});row.append(label,cards);reveal.append(row)});$('#board').append(reveal)}$('#hand').replaceChildren(...me.hand.map((c,i)=>{const e=cardEl(c);e.onclick=()=>{if(canAct()&&game.phase==='discard'){selected=i;render()}};if(selected===i)e.classList.add('selected');return e}));wireMovable(me.hand);$('#selectionHint').textContent=selected===null?'':'selected';$('#controls').innerHTML=game.rummyOver?'':game.turn!==myId?'':game.phase==='draw'?'<button class="primary" id="drawRummy">Draw card</button><button class="secondary" id="takeRummy">Take discard</button>':`<button class="primary" id="discardRummy" ${selected===null?'disabled':''}>Discard selected</button>`;$('#drawRummy')?.addEventListener('click',()=>rummyDraw(false));$('#takeRummy')?.addEventListener('click',()=>rummyDraw(true));$('#discardRummy')?.addEventListener('click',()=>rummyDiscard(selected))}
function findMeldPlan(hand){const key=h=>h.map(c=>c.rank+c.suit).sort().join(',');const memo=new Map();function combinations(arr,n,start=0,pick=[]){if(pick.length===n)return[pick];let out=[];for(let i=start;i<arr.length;i++)out.push(...combinations(arr,n,i+1,[...pick,arr[i]]));return out}function solve(left){if(!left.length)return[];let k=key(left);if(memo.has(k))return memo.get(k);let first=left[0],same=left.filter(c=>c.rank===first.rank);for(let size=3;size<=Math.min(4,same.length);size++){for(const set of combinations(same,size)){if(!set.some(c=>c.suit===first.suit))continue;let rest=left.slice();set.forEach(c=>rest.splice(rest.indexOf(c),1));let result=solve(rest);if(result){let answer=[set,...result];memo.set(k,answer);return answer}}}let suited=left.filter(c=>c.suit===first.suit),values=new Set(suited.map(c=>c.value));for(let start=2;start<=first.value;start++){for(let length=3;start+length-1<=14;length++){let sequence=[];for(let value=start;value<start+length;value++){let c=suited.find(x=>x.value===value);if(!c){sequence=[];break}sequence.push(c)}if(sequence.length>=3&&sequence.some(c=>c.value===first.value)){let rest=left.slice();sequence.forEach(c=>rest.splice(rest.indexOf(c),1));let result=solve(rest);if(result){let answer=[sequence,...result];memo.set(k,answer);return answer}}}}memo.set(k,null);return null}return solve([...hand])}function hasPureSequence(hand){for(const suit of ['♠','♥','♦','♣']){const vals=new Set(hand.filter(c=>c.suit===suit).map(c=>c.value));if(vals.has(14)&&vals.has(2)&&vals.has(3))return true;for(let start=2;start<=12;start++){if(vals.has(start)&&vals.has(start+1)&&vals.has(start+2))return true;if(vals.has(start)&&vals.has(start+1)&&vals.has(start+2)&&vals.has(start+3))return true}}return false}function canMeldAll(hand){return !!findMeldPlan(hand)}function canWinRummy(hand){return canMeldAll(hand)&&hasPureSequence(hand)}
function rummyDraw(takeDiscard){if(game.rummyOver||game.players[myId].finished)return;if(!sendAction('rummyDraw',{takeDiscard}))return;const p=game.players[myId],drawn=takeDiscard?game.discard.pop():drawCard();p.hand.push(drawn);game.lastAction=`${p.name} drew ${takeDiscard?'from the discard pile':'from the deck'}`;game.phase='discard';selected=null;render();sync()}
function completeRummy(index){const p=game.players[index];if(p.finished)return;p.finished=true;game.finishOrder.push({name:p.name,index});if(game.finishOrder.length>=game.players.length-1){const loser=game.players.find(x=>!x.finished);if(loser){loser.finished=true;game.finishOrder.push({name:loser.name,index:game.players.indexOf(loser),loser:true})}const weights=game.players.map((_,i)=>Math.max(0,game.players.length-i-1)),total=weights.reduce((a,b)=>a+b,0)||1;game.finishOrder.forEach((entry,i)=>{const prize=Math.floor(game.pot*weights[i]/total);game.players[entry.index].money+=prize;entry.prize=prize});game.rummyOver=true;game.winner=game.finishOrder[0].name;return}game.turn=(index+1)%game.players.length;while(game.players[game.turn].finished)game.turn=(game.turn+1)%game.players.length;game.phase='draw'}function rummyDiscard(index){if(game.rummyOver||index===null||index===undefined||game.players[myId].finished||!sendAction('rummyDiscard',{index}))return;const p=game.players[myId],c=p.hand.splice(index,1)[0];game.discard.push(c);game.lastDiscard=c;game.lastAction=`${p.name} discarded ${c.rank}${c.suit}`;selected=null;if(!p.hand.length||canWinRummy(p.hand)){completeRummy(myId);if(mode==='solo'||(mode==='online'&&isHost))runRummyBots()}else{game.phase='draw';game.turn=(myId+1)%game.players.length;if(mode==='solo'||(mode==='online'&&isHost))runRummyBots()}render();sync()}function botDelay(){return game?.botSpeed==='realistic'?2000+Math.floor(Math.random()*1001):450}function runRummyBots(){if(!(mode==='solo'||(mode==='online'&&isHost)))return;const step=()=>{if(game.rummyOver||!game.players[game.turn]?.bot)return;const i=game.turn;botRummy(i);render();sync();if(!game.rummyOver&&game.players[game.turn]?.bot)setTimeout(step,botDelay())};if(game.players[game.turn]?.bot)setTimeout(step,botDelay())}function botRummy(index=game.turn){const p=game.players[index];if(!p||p.finished||game.rummyOver)return;p.hand.push(drawCard());const plan=findMeldPlan(p.hand),level=game.botLevel||'sharp',outside=p.hand.map((c,i)=>({c,i})).filter(x=>!plan?.flat().includes(x.c));let remove=level==='casual'?Math.floor(Math.random()*p.hand.length):outside.length?outside.sort((a,b)=>level==='pro'?a.c.value-b.c.value:0)[0].i:Math.floor(Math.random()*p.hand.length);if(remove<0)remove=0;const discarded=p.hand.splice(remove,1)[0];game.discard.push(discarded);game.lastDiscard=discarded;game.lastAction=`${p.name} drew and discarded ${discarded.rank}${discarded.suit}`;if(!p.hand.length||canWinRummy(p.hand))completeRummy(index);else{game.turn=(index+1)%game.players.length;while(game.players[game.turn].finished)game.turn=(game.turn+1)%game.players.length;game.phase='draw'}}
function renderAce(){const me=game.players[myId];$('#status').textContent=game.winner?`${game.winner} wins the match.`:`Round ${game.round} of ${game.maxRounds} — ${game.turn===myId?'reveal your card':'opponent is revealing…'}`;$('#board').innerHTML=`<div class="board-stat"><b>${game.players[0].score}</b><span>${game.players[0].name}</span></div><h3>${game.winner||'Highest card wins the round'}</h3><div class="board-stat"><b>${game.players[1].score}</b><span>${game.players[1].name}</span></div>`;$('#hand').replaceChildren(...me.hand.map(c=>cardEl(c)));$('#selectionHint').textContent='';$('#controls').innerHTML=game.winner?'':game.turn===myId?'<button class="primary" id="revealAce">Reveal card</button>':'';$('#revealAce')?.addEventListener('click',()=>aceReveal())}
function aceReveal(){if(!sendAction('aceReveal')||game.phase!=='reveal')return;const a=game.players[0].hand[0],b=game.players[1].hand[0];if(a.value>b.value)game.players[0].score++;else if(b.value>a.value)game.players[1].score++;game.round++;if(game.round>game.maxRounds)game.winner=game.players[0].score>game.players[1].score?game.players[0].name:game.players[1].name;else{game.players.forEach(p=>p.hand=[drawCard()]);game.turn=0}render();sync();if(mode==='solo'&&!game.winner)queueBot()}
function unoLabel(c){return c.type==='wild4'?'+4':String(c.value).toUpperCase()}function unoCardEl(c){const e=document.createElement('button');e.className='playing-card uno-real-card';const img=document.createElement('img');img.src=`cards/uno/${c.color}_${c.value}.svg`;img.alt=`${c.color} ${unoLabel(c)}`;img.onerror=()=>{img.remove();e.innerHTML=`<b>${unoLabel(c)}</b><small>${c.color}</small>`};e.append(img);return e}
function legalUno(c){const top=game.discard.at(-1);if(game.drawStack){return game.drawStack===2&&c.type==='draw2'||game.drawStack===4&&c.type==='wild4'}if(c.type==='wild4'){const hasMatch=game.players[game.turn]?.hand?.some(x=>x!==c&&x.color===game.color);if(hasMatch)return false}return c.color==='wild'||c.color===game.color||c.value===top.value||c.type===top.type}
function renderUno(){const me=game.players[myId];$('#status').textContent=game.winner?`${game.winner} wins!`:game.turn===myId?'Play a matching card or draw.':`${current().name} is thinking…`;$('#board').innerHTML=game.players.map((p,i)=>`<div class="board-stat uno-seat ${i===myId?'you':''} ${i===game.turn?'active-seat':''}"><b>${p.hand.length}</b><span>${p.name}</span></div>`).join('')+`<div id="unoDiscard"></div>`;$('#hand').replaceChildren(...me.hand.map((c,i)=>{const e=unoCardEl(c);e.onclick=()=>{if(canAct()&&legalUno(c))unoPlay(i)};return e}));$('#unoDiscard').replaceChildren(unoCardEl(game.discard.at(-1)));wireMovable(me.hand);$('#selectionHint').textContent=me.hand.length===1?'Say UNO!':'';$('#controls').innerHTML=game.winner||game.turn!==myId?'':'<button class="primary" id="drawUno">Draw</button>';$('#drawUno')?.addEventListener('click',()=>unoDraw())}
function unoDraw(){if(!sendAction('unoDraw'))return;const p=game.players[myId];let n=game.drawStack||0;if(n){for(let i=0;i<n;i++)p.hand.push(drawCard());game.drawStack=0}else p.hand.push(drawCard());advanceUno();render();sync();if((mode==='solo'||(mode==='online'&&isHost))&&!game.winner)runUnoBots()}
function unoPlay(index,chosenColor=null){const p=game.players[myId],c=p?.hand[index];if(!c||!legalUno(c))return toast('That card does not match');if((c.type==='wild'||c.type==='wild4')&&!chosenColor){pendingWild=index;$('#colorModal').classList.remove('hidden');return}if(!sendAction('unoPlay',{index,color:chosenColor}))return;pendingWild=null;$('#colorModal').classList.add('hidden');p.hand.splice(index,1);game.discard.push(c);if(c.color!=='wild')game.color=c.color;else game.color=chosenColor||'red';if(c.type==='draw2')game.drawStack=(game.drawStack||0)+2;if(c.type==='wild4'){game.drawStack=(game.drawStack||0)+4;if(kind==='uno-flip')game.dark=!game.dark}if(!p.hand.length)game.winner=p.name;else{if(c.type==='skip'||c.type==='reverse')game.turn=(game.turn+1)%game.players.length;advanceUno()}render();sync();if((mode==='solo'||(mode==='online'&&isHost))&&!game.winner)runUnoBots()}
function advanceUno(){game.turn=(game.turn+1)%game.players.length}
function runUnoBots(){if(!(mode==='solo'||(mode==='online'&&isHost)))return;const step=()=>{if(game.winner||!game.players[game.turn]?.bot)return;botUno(game.turn);render();sync();if(!game.winner&&game.players[game.turn]?.bot)setTimeout(step,botDelay())};if(game.players[game.turn]?.bot)setTimeout(step,botDelay())}function botUno(index=game.turn){const p=game.players[index];if(!p||game.winner)return;let i=p.hand.findIndex(legalUno);if(i<0){let n=game.drawStack||1;for(let x=0;x<n;x++)p.hand.push(drawCard());game.drawStack=0}else{const c=p.hand.splice(i,1)[0];game.discard.push(c);if(c.color!=='wild')game.color=c.color;else{const counts={};p.hand.forEach(x=>{if(x.color!=='wild')counts[x.color]=(counts[x.color]||0)+1});game.color=Object.entries(counts).sort((a,b)=>b[1]-a[1])[0]?.[0]||'red'}if(c.type==='draw2')game.drawStack=2;if(c.type==='wild4'){game.drawStack=4;if(kind==='uno-flip')game.dark=!game.dark}if(c.type==='skip'||c.type==='reverse')game.turn=(index+2)%game.players.length;else game.turn=(index+1)%game.players.length;if(!p.hand.length)game.winner=p.name;return}game.turn=(index+1)%game.players.length}
function queueBot(){clearTimeout(botTimer);if(mode==='solo'&&game&&!game.winner&&game.turn===1)botTimer=setTimeout(()=>{if(kind==='rummy')botRummy();else if(kind==='uno')botUno();else {game.turn=0;render()}},450)}
function handleRemote(action,data,actor){const before=myId;myId=actor;if(action==='aceReveal')aceReveal();else if(action==='rummyDraw')rummyDraw(data.takeDiscard);else if(action==='rummyDiscard')rummyDiscard(data.index);else if(action==='unoDraw')unoDraw();else if(action==='unoPlay')unoPlay(data.index,data.color);myId=before;render()}
function hostRoom(){if(typeof Peer==='undefined')return $toast('Online service unavailable');isHost=true;const code=Math.random().toString(36).slice(2,8);peer=new Peer(code);peer.on('open',()=>{newGame('online');$('#roomLabel').textContent='ROOM '+code.toUpperCase()});peer.on('connection',c=>{conn=c;c.on('open',()=>sync());c.on('data',d=>{if(d.type==='action'){handleRemote(d.action,d.data||{},1);sync()}})})}
function joinRoom(){if(typeof Peer==='undefined')return $toast('Online service unavailable');const code=$('#roomCode').value.trim().toLowerCase();if(!code)return $toast('Enter a room code');isHost=false;myId=1;peer=new Peer();peer.on('open',()=>{conn=peer.connect(code);conn.on('open',()=>{$('#roomLabel').textContent='CONNECTED';$toast('Joined room')});conn.on('data',d=>{if(d.type==='state'){game=d.game;showGame();render()}})})}
setConfig();$$('.mode').forEach(b=>b.onclick=()=>{$$('.mode').forEach(x=>x.classList.remove('active'));b.classList.add('active');mode=b.dataset.mode;$('#onlineSetup').classList.toggle('hidden',mode!=='online');$('#startSolo').classList.toggle('hidden',mode==='online')});$('#startSolo').onclick=()=>$('#botModal').classList.remove('hidden');$('#cancelBots').onclick=()=>$('#botModal').classList.add('hidden');$('#confirmBots').onclick=()=>{$('#botModal').classList.add('hidden');newGame('solo')};$('#restart').onclick=()=>newGame(mode);$('#hostRoom').onclick=hostRoom;$('#joinRoom').onclick=joinRoom;$$('.color-choice').forEach(b=>b.onclick=()=>{if(pendingWild!==null)unoPlay(pendingWild,b.dataset.color)});
